#!/usr/bin/env python3
"""The supported research evidence and development command."""

import json
from pathlib import Path
import sys

_PLUGIN_ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(_PLUGIN_ROOT))
_PLUGIN_VERSION = json.loads((_PLUGIN_ROOT / ".claude-plugin/plugin.json").read_text())["version"]
_USER_AGENT = "exactory-research/" + _PLUGIN_VERSION

from research_harness.cli import main


if __name__ == "__main__":
    main()
