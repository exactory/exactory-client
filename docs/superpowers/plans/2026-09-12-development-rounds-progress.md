# Development rounds: progress and resume record

This file is the durable checkpoint of the implementation of
[the development rounds plan](2026-09-12-development-rounds.md) against
[the spec](../specs/2026-09-12-development-rounds-design.md). A session can
stop at any moment (usage limit, crash, user interrupt). Whoever resumes reads
this file first, then continues from the first unchecked item. Every agent that
finishes a task, a fix or a release step updates this file in the same commit as
the work, so the file never lags the branch.

Worktree: `/Users/ryshiro/exactory/plugins/exactory-client-worktrees/development-rounds`,
branch `feat/development-rounds`, base `main` at `19ef5d2` (0.38.0).
Runner: `python3 -m unittest discover -s tests -p <test_file.py>` from the
worktree (the `tests.<module>` form does not import; the fixtures are top-level
modules). Full suite: `python3 -m unittest discover -s tests` (about 17 minutes,
baseline 901 tests OK in 1025 s). Commit messages go through a file
(`git commit -F`), with the attribution lines the session provides.

## How to resume

1. Read this file and `git log --oneline main..HEAD` in the worktree. The branch is
   the truth; this file says what the commits mean and what is still owed.
2. Confirm the tree is clean (`git status --short`). Uncommitted work from an
   interrupted agent is inspected, finished or reverted before anything else.
3. Continue at the first unchecked item below, in order. Each plan task keeps the
   plan's TDD steps; each task ends with two independent reviews (specification
   conformance; code quality and test rigor) and fixes for every blocker and
   major finding, then a commit that also updates this file.
4. An orchestrating workflow may be used (one implementer, then two reviewers in
   parallel, then a fixer, per task; never more than two agents at once). Pass the
   first pending task number as `args.start` and the pending findings below as
   the first fix. If the workflow dies, this file is still current because every
   agent commits it with its work.

## Task status

| Task | Title | Status | Commits |
| --- | --- | --- | --- |
| 1 | Resource purpose `development` and unit `rounds` | done, reviewed | `faa7b71`, `a0e72f3` |
| 2 | The `next_round` disposition and carried developments | done, reviewed | `2d46d94` |
| 3 | Objective lineage | done, reviewed | `e92b4aa`, `fdfb4ba`, `92ec000`, `97d536f` |
| 4 | `rounds.py`: the round decision and its independent review | done, reviewed four times, fixed four times (fourth fixes in the commit that carries this row) | `523beb2`, `987806e`, `4d352c7`, `5399725`, `e9a8757` |
| 5 | `round-admit` opens the next round | done, reviewed twice, fixed twice (second fixes in the commit that carries this row) | `524a97b`, `f0c512c`, `d937f32` |
| 6 | Consequence purposes and the exemplar reading in the literature stage | done, reviewed twice, fixed twice (second fixes in the commit that carries this row) | `7164b86`, `dc62aee`, `75e7360` |
| 7 | `manuscript-prediction`: the blind cohort prediction | done, reviewed twice, fixed twice (second fixes in the commit that carries this row) | `9aa8f42`, `a16376e`, `0020c72` |
| 8 | `round-assess` and the round's derived progress | done, reviewed four times, fixed four times (fourth fixes in the commit that carries this row) | `e10cc3a`, `beef888`, `ecfd8a0`, `c011149`, `43c26fa` |
| 9 | The `round` gate, the stage transitions and the status report | done, reviewed twice, fixed twice (second fixes in the commit that carries this row) | `e35d24a`, `4f41572`, `3b683b0` |
| 10 | The round packet and `export --kind round` | implemented (the packet and its delivery; the CLI kind is Task 11's), review pending | `c9db1a4`, and the implementation commit that carries this row |
| 11 | CLI commands, example catalog and CLI documentation | pending | |
| 12 | Constitution Version 3, skills, README, version 0.39.0, release note and testing record | pending | |

## Checklist

- [x] Task 1 implemented, reviewed, fixed (legacy seven-unit accounts stay readable through `_account`)
- [x] Task 2 implemented, reviewed
- [x] Task 3 implemented, reviewed, fixed twice (null wider objective refused; lineage contract recorded; reused ancestor id refused)
- [x] Task 4 implemented and first review fixed (`cycle_authors` union for independence; reopening, stop and review bindings)
- [x] Task 4 second-review findings fixed (see "Pending findings" below), tests green (`test_research_rounds.py` 19, `test_research_publication.py` 8, `test_research_review_packets.py` 20), this file updated
- [x] Task 4 third-review findings fixed (see "Third review of Task 4" below), tests green (`test_research_rounds.py` 20, `test_research_development.py` 65, `test_research_publication.py` 8, `test_research_review_packets.py` 20), this file updated
- [x] Task 4 fourth-review findings fixed (see "Fourth review of Task 4" below), tests green (`test_research_rounds.py` 21, `test_research_publication.py` 8, `test_research_review_packets.py` 20), this file updated
- [x] Task 5 implemented (RED then GREEN; `test_research_rounds.py` 25, `test_research_resources.py` 9; `git diff --check` and `codex/generate.py --check` clean), this file updated
- [x] Task 5 reviewed, fixed (see "Review of Task 5" below), tests green (`test_research_rounds.py` 31), `git diff --check` and `codex/generate.py --check` clean, this file updated
- [x] Task 5 second-review findings fixed (see "Second review of Task 5" below), tests green (`test_research_rounds.py` 32), `git diff --check` and `codex/generate.py --check` clean, this file updated
- [x] Task 6 implemented (RED then GREEN; `test_research_rounds.py` 35, `test_research_synthesis.py` 33, `test_research_literature.py` 23; `git diff --check` and `codex/generate.py --check` clean), this file updated
- [x] Task 6 reviewed, fixed (see "Review of Task 6" below), tests green (`test_research_rounds.py` 37, `test_research_synthesis.py` 33, `test_research_literature.py` 23), `git diff --check` and `codex/generate.py --check` clean, this file updated
- [x] Task 6 second-review finding fixed (see "Second review of Task 6" below; tests only, no code change), tests green (`test_research_rounds.py` 37, `test_research_synthesis.py` 33, `test_research_literature.py` 23), `git diff --check` and `codex/generate.py --check` clean, this file updated
- [x] Task 7 implemented (RED then GREEN; `test_research_rounds.py` 39; `git diff --check` and `codex/generate.py --check` clean), this file updated
- [x] Task 7 reviewed, fixed (see "Review of Task 7" below), tests green (`test_research_rounds.py` 39, `test_research_publication.py` 8, `test_research_review_packets.py` 20), `git diff --check` and `codex/generate.py --check` clean, this file updated
- [x] Task 7 second-review findings fixed (see "Second review of Task 7" below), tests green (`test_research_rounds.py` 39, `test_research_publication.py` 8, `test_research_review_packets.py` 20), `git diff --check` and `codex/generate.py --check` clean, this file updated
- [x] Task 8 implemented (RED then GREEN; `test_research_rounds.py` 44, `test_research_literature.py` 23, `test_research_synthesis.py` 33, and for the shared fixture change `test_research_gates.py` 7, `test_research_publication.py` 8, `test_research_execution.py` 16; `git diff --check` and `codex/generate.py --check` clean), this file updated
- [x] Task 8 reviewed, fixed (see "Review of Task 8" below), tests green (`test_research_rounds.py` 46), `git diff --check` and `codex/generate.py --check` clean, this file updated
- [x] Task 8 second-review findings fixed (see "Second review of Task 8" below), tests green (`test_research_rounds.py` 46, `test_research_publication.py` 9, `test_research_development.py` 65), `git diff --check` and `codex/generate.py --check` clean, this file updated
- [x] Task 8 third-review findings fixed (see "Third review of Task 8" below), tests green (`test_research_rounds.py` 47, `test_research_literature.py` 23), `git diff --check` and `codex/generate.py --check` clean, this file updated
- [x] Task 8 fourth-review findings fixed (see "Fourth review of Task 8" below), tests green (`test_research_rounds.py` 48), `git diff --check` and `codex/generate.py --check` clean, this file updated
- [x] Task 9 implemented (RED then GREEN; `test_research_rounds.py` 58, `test_research_report_views.py` 12, `test_research_gates.py` 7, `test_research_cli.py` 18, and for the `status_report` change `test_research_evaluation.py` 4 and `test_research_resources.py` 9; `git diff --check` and `codex/generate.py --check` clean), this file updated
- [x] Task 9 reviewed, fixed (see "Review of Task 9" below), tests green (`test_research_rounds.py` 59, `test_research_report_views.py` 12, `test_research_gates.py` 7, `test_research_cli.py` 18), `git diff --check` and `codex/generate.py --check` clean, this file updated
- [x] Task 9 second-review findings fixed (see "Second review of Task 9" below), tests green (`test_research_rounds.py` 59, `test_research_report_views.py` 12, `test_research_gates.py` 7, `test_research_cli.py` 18), `git diff --check` and `codex/generate.py --check` clean, this file updated
- [x] Task 10 implemented (RED then GREEN; `test_research_review_packets.py` 24; `git diff --check` and `codex/generate.py --check` clean), this file updated
- [ ] Task 10 reviewed, fixed
- [ ] Task 11 implemented, reviewed, fixed
- [ ] Task 12 implemented, reviewed, fixed
- [ ] Full suite green (`python3 -m unittest discover -s tests`), count and time recorded in `docs/testing/development-rounds.md`
- [ ] Whole-branch two-reviewer pass (record integrity and gate semantics; specification conformance), every blocker and major fixed with a regression test, findings recorded in the testing record
- [ ] `python3 codex/generate.py --check` and `git diff --check` clean; full suite again if code changed
- [ ] Branch pushed; PR to `main` in `exactory/exactory-client` opened with the release summary and attribution; six CI jobs green; merged
- [ ] `local-dev` and `dev` fast-forwarded to the merge commit; tag `exactory--v0.39.0` ("Exactory 0.39.0"); GitHub release created from `docs/releases/0.39.0.md` with absolute links
- [ ] `main` pulled in the primary checkout `plugins/exactory-client`; the closed-gravity study's next command reports `constitution_revalidation_required`

## Deviations from the plan so far

- Tests run with `python3 -m unittest discover -s tests -p <file>`; the plan's `tests.<module>` form does not import.
- Task 1: `tests/test_research_resources.py` has a module-level `limits()` helper, not a method; a legacy `resource_account` persisted with seven units is normalized at the read seam (`resources._account`) so upgraded stores keep working.
- Task 3: `principles._validate_objective` was extracted; `widen_objective` refuses a null target, an unchanged statement, and any objective id that already exists (the current one or an ancestor); the lineage record carries the containment contract.
- Task 4: `development.cycle_authors(records)` is the union of plan and assessment authors and is used by readiness, round reviews and (later) predictions; a stop decision on a bundle is refused while an admitted round is unassessed; reopening and review bindings follow the spec text.
- Task 4, second fixes: `rounds._carried` selects the closing round's carried developments by assessment time (`cycle_assessment.assessed_revision` after the latest admission's `admitted_revision`), not by the plan's cycle-id filter, so a re-assessment of an earlier round's cycle is disposed of by the round gate (spec 1 and 5.1). `development.carried_developments` first kept its `cycle_ids` parameter; the third review removed it (see below). Task 5's `admit_round` must write `admitted_revision` on the admission (spec 5.3 lists it); `opening.cycle_ids` stays for the freshness rules of Tasks 8 and 10. The fixture `write_round` writes `admitted_revision` instead of `opening.cycle_ids`, and `write_round_assessment` writes a round's assessment on its own (both through `write_record`). A reopening names a round assessed unsuccessful; a successful round's goal stays `round_goal_repeated` with no reopening path.
- Task 4, third fixes: `development.carried_developments(records)` has no `cycle_ids` parameter (the plan's Task 2 signature had one; its only production caller, `rounds._carried`, selects by assessment revision instead, so the parameter was dead surface, CLAUDE.md sections 6 and 9-I). The deferred-candidate permission of spec 5.1 has a regression test.
- Task 4, fourth fixes: spec 5.1's sentence "`round-review` refuses to approve a second decision for a closing round that already has an approved one (`round_decision_duplicate`)" is applied per bundle: `record_round_review` refuses the approval only when the earlier approved decision binds the current bundle. The spec's own gate (section 6 item 3, the plan's Task 5 `admit_round` with `round_review_stale`, Task 9 `_decision_obligations`) reads only a decision on the current bundle, so an approved decision on a superseded bundle is unreachable for the gate, and the literal reading left a study with no exit after the section 9 park (stop approved on bundle X, publication gate fails, the user revises and pins Y, the stop on Y cannot be approved), contradicting section 10's complete exit table. This narrows a spec rule, so it is reported to the user (CLAUDE.md product philosophy 3). The error text now reads "This closing round already has an approved decision on this bundle". Names: `admitted` became `admitted_revision` in `_carried`; `reopened` became `reopened_round_id` in `_next`, `_distinct` and `_direction_open` (CLAUDE.md sections 12 and 13; no behavior change).
- Task 5: the plan's tests were appended unchanged. Four deviations in `rounds.admit_round` from the plan's reference code were recorded; the review withdrew the first, so three remain. (1) Withdrawn: `opening.cycle_ids` reads `records["cycle"]` as the plan wrote it. Commit `f0c512c` read `cycle_plan` and its message and the earlier version of this sentence claimed that the plan's `cycle` record kind does not exist. That claim was false: `development.plan_cycle` writes `cycle_plan` (the immutable plan) and `cycle` (the mutable status projection: `status`, `execution_ids`, `assessment_id`) in one mutation with the same id, and `cycle` is the kind `admit_cycle`, `assess_cycle`, `carried_developments` and the plan's Tasks 8 and 10 read. The two id sets are identical, so the shipped behavior was the plan's; only the recorded reason was wrong. The review of Task 5 corrected the code to the plan and this record; the message of `f0c512c` keeps the wrong reason because history is not rewritten. (2) The re-admission check is split: `round_active` when the latest round is unassessed (the tested case), and `round_number_mismatch` when the decision does not close the current round (the plan folded the second case, a decision whose round was already assessed, into `round_active` through an `any(a["decision_id"] == ...)` scan; the number check states the spec 5.1 invariant directly and also refuses any stale decision). (3) The review's `round_digest` is not compared with the decision digest again (`record_round_review` bound it and decisions are immutable; CLAUDE.md section 2). (4) `resources.charge` is appended without the plan's `None` guard (`_prepare_context` requires a configured research profile, so `charge` never returns `None` there). One change outside the task's file list: `literature._foundation_state` leaves the `development` purpose out of the resource obligations it merges (`if o["purpose"] != "development"`). Without it the plan's `test_an_exhausted_development_budget_refuses_the_decision` failed at the pin with `readiness_required`, and a live study with a `development` budget at its limit (after the admission that reached the limit) would have had its foundation, readiness and manuscript pin blocked for the whole round. Spec section 12 states the exhaustion is "an obligation and a checkpoint condition, never readiness"; `rounds._next` refuses the decision on it, and Task 9's gate report and status carry it. No existing test asserted the development purpose in foundation or status obligations (`test_research_resources.py` covers the literature purpose only), so no test changed. The plan's `_opening` signature is kept (`records, evaluation, bundle`), called with `context.artifacts`, the shared `Evaluation`.
- Task 5, second fixes (tests only, no code change): `test_admission_needs_a_review_of_this_decision` covers the two admission refusals the plan's tests left out, `unknown_round_decision` and the review-binding half of `round_review_required` (an approval of one decision cannot open another decision's round). `test_admission_needs_an_approved_continue_decision_and_records_the_opening_state` now asserts the opening state's `requirement_ids`, `reading_count` and `accounts` against the store as it stood before the admission (the plan's version checked only `bundle_id`, `claim_ids`, `cycle_ids` and `search_selection`). `requirement_ids` is empty in that fixture: a full-text requirement before the pin fails the pin with `readiness_required`, one after the pin fails the admission with `publication_readiness_stale`; the non-empty case is asserted by Task 6's `test_an_exemplar_the_round_opened_with_does_not_count` (a requirement, the five searches, the sections and a readiness review before the pin).
- Task 6: the plan's tests were appended with the changes below; the four new tests failed with `invalid_search` before the implementation (`7164b86`). (1) Judgments. The plan's reference code let the development purposes into `selected_searches`, and so into the judgments digest, only while a round is active. That digest is bound by the `rationale`, `innovation` and `context` sections and, through `literature_digest`, by cycle plans and assessments, so under the plan's code everything recorded inside a round would have gone stale at `round-assess` (the round is no longer active) and readiness, and therefore any re-pin at the round gate, would have failed. Shipped rule: a development selection is a judgment when it differs from the latest admission's opening selection (a search recorded in the current round, whether the round is active or assessed); before any admission no development search is a judgment; the two obligations `round_search_missing` and `round_exemplar_missing` apply only while the round is active, as the plan wrote them. The two behaviors are asserted in `test_a_development_purpose_is_a_search_purpose` (the early search leaves `literature_digest` unchanged) and `test_development_searches_enter_the_judgments_that_synthesis_depends_on` (the digest is unchanged after the round's assessment). The freshness rule reads the opening selection rather than a record revision because search records carry none. (2) `SynthesisCase.record_purpose` was extracted in this task (the plan's fixture section said Task 8; the task's file list said Task 6); the rounds fixture's identical local copy was removed, so `RoundsCase` inherits it. (3) The plan's `RoundLiteratureTests.codes()` override is not needed: `LiteratureCase.codes()` already returns the set of foundation obligation codes. (4) `test_an_active_round_requires_fresh_consequence_searches_and_an_exemplar` records a fresh readiness review after the early search: `development._source_inputs` collects every `source_id` in the foundation report, whose `search_history` lists every search of the profile, so any newly recorded search stales the readiness review (existing behavior, independent of this task) and the plan's pin failed with `readiness_required`. (5) Three existing tests (`test_a_late_reassessment_of_an_earlier_cycle_is_carried_by_the_closing_round`, `test_an_admitted_round_is_assessed_before_the_next_decision`, `test_admission_needs_an_approved_continue_decision_and_records_the_opening_state`) pinned, decided or re-admitted inside an active round. Spec section 7 makes the round's searches and exemplar foundation obligations, so readiness and the pin fail until the round's literature work is done. The fixture gained `round_literature(suffix)`: the exemplar requirement first (a full-text requirement joins the works every search judgment rests on, `literature._relevant_versions`, so the five purposes are re-judged, spec 7), then the four consequence searches, then the sections re-recorded through `refresh_synthesis`; the first two tests call it, re-assess the candidate (`recandidate`) and pin again before the assertion the plan wrote. (6) `admit_round` checks `round_active` before `_prepare_context`. Under the plan's order the branch was unreachable: during an active round readiness fails (the round's obligations) until the round's literature work is done, and once it is done the decision's bundle is no longer current (`round_review_stale`). A re-admission during an active round is now refused as `round_active` before readiness; the third test above covers it. (7) The fixture `write_round` writes the real `opening` state through `rounds._opening`, which the literature rule reads. (8) The docs and skills that say "five search purposes" (`README.md`, `docs/research-cli.md`, `skills/*`) are left to Tasks 11 and 12.
- Task 6, review fixes: `literature._foundation_state` emits `round_search_missing` in its own step (every development purpose absent from `selected_searches`, only while a round is active) and runs the scope, dispositions, evidence and frontier checks over every entry of `selected_searches`, so a consequence search recorded in the current round is checked after `round-assess` as well (the shipped loop iterated `SEARCH_PURPOSES + DEVELOPMENT_PURPOSES if active` and skipped the round's consequence judgments once the round was assessed, while `judgments_digest` and the report still carried them). The task's interface sentence ("the same scope, dispositions, evidence and frontier checks as the five purposes") is now true in the window between the assessment and the next admission. Two test gaps closed: a fresh consequence search is judged beside the five purposes (`search_evidence_stale` on all six after a new full-text requirement, cleared by re-recording it), and an exemplar requirement that predates the admission earns no credit (`opening.requirement_ids == ["exemplar-early"]`, `round_exemplar_missing` until the round's own requirement).
- Task 7: `research_harness/predictions.py` follows the plan's reference code with these changes. (1) The plan's "move `_authors` from `rounds.py` to `publication.authors(records)`" has nothing to move: Task 4 replaced `rounds._authors` with `development.cycle_authors(records)`, so `record_prediction` checks the assessor against `development.cycle_authors(records)` and no `publication.authors` exists. That live set is the union of every cycle plan and assessment author; the bundle's frozen `candidate.authors`, which `record_manuscript_review` checks, is the same function's value at pin time, so the live set is a superset (records are append-only) and a prediction is refused for at least every author a manuscript review is refused for. (2) `validate_assessor`'s first parameter is the artifact reader; the `Evaluation` built in `prepare` is passed, as `record_manuscript_review` and `record_round_review` do. (3) The plan's two tests were appended with additions: a prediction before any pin is `publication_bundle_missing`; the record's `bundle_digest` is asserted; a band value outside 1 to 100 (`worst: 101`) is `invalid_prediction`; the summary's `contribution` measure and the empty bundle's `reviews` count and `overall` measure are asserted. The RED failure was `ImportError: cannot import name 'predictions'` (the test module imports `predictions` at the top with `resources` and `rounds`, not inside each test as the plan wrote). (4) `tests/rounds_fixtures.py` imports `predictions` at the module top; the inline import in `measure` was the stand-in while the module did not exist. (5) Names only: `_COHORT_ERROR` and `_SCORE_KEYS` are module constants. The `review_not_independent` branch for an author was checked by emptying the author set in the worktree: the test fails with `ResearchError not raised`; the shipped file was restored byte-for-byte. `reviewed_revision` is `expected_revision + 1` as the plan wrote (round records use the same; manuscript reviews record `expected_revision`, a pre-existing difference this task does not touch).
- Task 7, review fixes: the "latest review per assessor on the bundle" selection has one home, `publication.latest_reviews(records, bundle_digest)`, returning `{assessor_key: saved}`; `publication_state` and `predictions.measurement_summary` both call it. `publication_state` keeps reading every review's rubric core from the hash-verified artifact through `_review` (every review of the bundle, as before) and takes the accept decision from that artifact core, keyed by review id, not from the stored `core` field the finding proposed: `tests/test_research_review_packets.py`'s `test_one_review_per_assessor_per_bundle_and_a_rejection_stands` writes a legacy record whose stored `core` is the copied rejection while its `review` artifact is an acceptance, and the gate must go on reading the artifact (the gate trusts verified bytes, not a stored copy). The summary reads the stored `core`, which for every record written through `record_manuscript_review` equals the artifact's content. `test_measurement_summary_reports_medians_and_spreads` now pins the selection: a later review by the same assessor under a casefold-equal id (`Measure-One-1`, `reviewed_revision` above the store's) is selected (count 5, overall spread `[6, 10]`), an earlier one (`reviewed_revision` 0) is not (unchanged), and `publication_report` selects the same record.
- Task 7, second-review fixes (no behavior change): the rubric's numeric score vocabulary has one home, `publication.SCORE_SCALES = (("soundness", 4), ("presentation", 4), ("contribution", 4), ("overall", 10))`; `publication._review` iterates it and `predictions.measurement_summary` imports it (`for key, _ in SCORE_SCALES`), so the plan's `_SCORE_KEYS` copy in `predictions.py` is gone. The plan's `_cohort(records)` helper is gone as well: its `if collection is not None` skip and its trailing `prediction_cohort_mismatch` raise ("no frozen cohort") were unreachable, because `_prediction` runs only after `_bundle` returned, and `_bundle` requires `author_readiness_state(...)["ready"]`, which is false whenever `_foundation_state`'s `cohort_report` emits `cohort_missing` (an empty research `collection_ids`, or any id without a `collection` record) on the same snapshot. `_prediction` now reads the first collection of the research scope directly (`records["collection"][records["literature_scope"]["research"]["collection_ids"][0]]["definition"]`), and `prediction_cohort_mismatch` keeps its one reachable meaning (a prediction against another corpus, category or window), tested at `test_a_prediction_binds_the_bundle_the_cohort_and_a_blind_assessor`. One assertion added to `test_measurement_summary_reports_medians_and_spreads`: the reviews summary carries exactly `count` and the four rubric keys.
- Task 8: `rounds.derive_progress` and `rounds.assess_round` follow the plan's reference code with these changes. (1) The freshness rules have one home: `rounds.fresh_searches(records, admission)` (`{purpose: search_id}` for every development purpose whose selected search differs from the opening one) and `rounds.has_round_exemplar(records, admission)`; `literature._foundation_state` calls them instead of its own copies of both rules (the plan's `derive_progress` restated them; CLAUDE.md section 2b). `has_round_exemplar` keeps the `profile == "research"` filter of literature's `full_requirements`, which the plan's copy lacked. `fresh_purposes` is a sorted list. (2) The plan's check "Assess the current round" (`latest_admission(records)["id"] != admission["id"]`) was first left out of `assess_round` as unreachable (every non-latest admission has an assessment, so `round_already_assessed` was raised first). The third-review fix (`43c26fa`) narrowed `round_already_assessed` to the current bundle, which made the check reachable again (an earlier round's assessment on a later round's bundle), so the fourth-review fix restored it as the plan wrote it; see "Task 8, fourth-review fixes". (3) Names only: `CRITERION_STATUSES` (the plan's `STATUSES`), `_validate_judgments` (`_judged`), `_compute_usage` (`_charged_since`) (CLAUDE.md sections 12 and 14). (4) The fixture `run_round_work` calls the existing `round_literature(suffix, read_exemplar=True)` (the plan's four inline lines were already that helper) and the new `read_exemplar` records a full reading of the exemplar's bundle inside the round, so `derived["readings"]` is asserted as 1 (spec global constraints: at least one full reading of an exemplar per round). (5) The new cycle runs through the actual launcher: `integration_fixtures.observe_run(case, plan=, script=, run_id=, request_id=)` is the launch step extracted from `observed_candidate` (whose behavior is unchanged; `OBSERVED_PROGRAM` is the module constant) and `admit_lab` accepts a `plan`. The plan's `self.run_cycle` records the execution without the launcher's claim, and readiness then reports `execution_observation_required` for the managed run, so the plan's pin failed with `readiness_required` (found by a scratchpad probe before the RED run). (6) Tests beyond the plan: `unknown_round`, a duplicated criterion and an unknown status (`invalid_round`); the record's `round_id`, `bundle_digest` and `assessed_revision`; `measurement` equals `predictions.measurement_summary`; `usage` through a real `resources.charge` of 3 literature network requests inside the round and the admission's own `development` charge of 1 round (the opening accounts predate the admission's charge, so the round's usage carries it); `test_a_productive_round_without_an_observed_criterion_is_unsuccessful` (successful False, unproductive False); `derive_progress(..., None)` reports empty claim lists, no measurement, the round's cycles, and `unproductive` True. Two earlier drafts of the usage assertion guessed at other modules' charging (a full reading through `record_reading` charges no `readings` unit; the fixture's `mcp` captures charge no network requests) and were replaced by the explicit charge, so the test pins `derive_progress`, not the charging of other modules.
- Task 8, review fixes: (1) `derive_progress` reports `changed_claim_ids`, a key the plan's interface list (Task 8 "Produces") does not have: the opening claims whose `claim` text differs from the opening bundle's `evidence/claims.json` and that carry neither `revised` nor `superseded` (empty when `bundle` is None). Spec section 9 states the continuity rule as "every opening claim id is present with the same text, or present with `revised`, or present with `superseded`"; the plan's reference code and the shipped code compared ids only, so a rewritten claim without a marker was neither dropped nor revised. The opening bundle's claims are read from `records["publication_bundle"][opening["bundle_id"]]` through the same `Evaluation`. This follows the spec over the plan, so it is reported to the user (CLAUDE.md product philosophy 3); if the user confirms the plan's id-only reading, the key and its test are removed. Task 9 folds `changed_claim_ids` into `round_claims_dropped` (or a sibling code) when it builds `_progress_obligations`; the plan's reference code (plan lines 1926-1928) reads `dropped_claim_ids` alone. (2) `_validate_judgments` validates each criterion id as text (`_text(item["id"], name + " ID")`) before the membership test, as `_goal` and `_candidates` do; a list id is `invalid_round` instead of a `TypeError` traceback from `cli.main`. (3) Tests only: `open_round` takes a prepared decision `payload` (keyword) so a test can open a round whose goal has two success criteria; the new `test_every_criterion_of_a_two_criterion_goal_is_judged_with_evidence` pins the "judge every one of the goal's criteria" check and the evidence clause (spec 5.4), and accepts an assessment with `sc-scope` `unresolved` (the first accepted use of that status); the new `test_an_unproductive_round_lacks_a_fresh_search_or_the_round_exemplar` pins the `fresh` and `exemplar` legs of `unproductive` on the pure function with a copied admission whose opening state already holds the round's `changes` search or its exemplar requirement.
- Task 8, second-review fixes: (1) The round's readings are its full readings. `rounds.count_full_readings(records)` (readings whose `depth` is `fulltext`) has two callers: `_opening` records it as `fulltext_reading_count` and `derive_progress` reports `readings` as the difference. The plan's expression (`len(records["reading"]) - opening["reading_count"]`, plan lines 1178 and 1683) counted every depth, so a `field_change` round's abstract readings of the added cohort members (spec 7) were reported as the round's readings. Spec 5.4 defines the stored value as "the count of cycles, searches and full readings recorded since the round's admission" and the global constraints tie the round to "at least one full reading of a development exemplar"; spec 5.3 and 14 say "reading count" and "readings". The opening key is renamed from the plan's `reading_count` (plan line 1081) because it now holds a count of full readings (CLAUDE.md section 12); no later plan task reads the key (Tasks 9 and 10 read `derived`). This follows spec 5.4 over the plan, so it is reported to the user; if the user confirms the plan's all-depth count, the helper's filter, the key name and the regression assertion are reverted. The Task 5 assertion on the opening state restates the new definition; its value in that fixture is unchanged (the six readings of `prepared_study` are all full readings, checked by a scratchpad probe). (2) One change outside the task's file list: `publication._claim_markers` validates the continuity markers at the pin, where the claim registry is already validated (CLAUDE.md section 2): `revised` is exactly `{previous, reason}` with nonblank text, `superseded` is exactly `{reason}` with nonblank text, and a claim carries at most one of the two; every refusal is `publication_claims_missing`. The finding's second half, closing the claim record to `id`, `claim` and the two markers, was not applied: the claim ledger is documented as an open record (`skills/write/WORKSPACE.md`, "evidence/claims.json: the claim ledger", shows `source` and `note` keys and asks for scope, assumptions, units and more per claim), and `tests/test_research_publication.py` pins a claim with a `source` key in its `setUp`, so a closed record would refuse every documented ledger. `derive_progress` keeps its key-presence reads because the pin now guarantees the shape. (3) Every record id a payload names is checked as text before it is looked up: `round_id` in `assess_round`, `record_round_review` and `_reopening`, and `round_id` and `review_id` in `admit_round` (the class of the first review's Finding 5, swept per CLAUDE.md section 13). (4) The observation status vocabulary has one home, `development.OBSERVATION_STATUSES`, used by `_assessed_checks` and by `rounds._validate_judgments`; `rounds.CRITERION_STATUSES` is gone (CLAUDE.md section 2b). No behavior change. (5) Tests only: the `cycles` leg of `unproductive` is pinned on the pure function (the test was renamed to `test_an_unproductive_round_lacks_a_fresh_search_the_round_exemplar_or_a_new_cycle`).
- Task 8, third-review fixes: (1) Spec 5.4's sentence "One assessment per round" is applied per bundle, as the fourth review of Task 4 applied `round_decision_duplicate`: `assess_round` refuses `round_already_assessed` only when the round's latest assessment binds the current bundle, and `rounds.assessment_for` returns the round's latest assessment (the greatest `assessed_revision`), so `record_round`, `active_round`, `_reopening` and `_direction_open` read the current judgment. Under the literal reading a round of number 2 or more whose assessment bound a superseded bundle had no exit: `record_round` requires an assessment on the current bundle (`round_assessment_missing`, spec 5.4 "a prerequisite of the next `round` decision") and `assess_round` refused every second assessment. That is the spec section 9 park (a stop whose bundle fails the publication gate, the user revises and pins again) and the ordinary mistake of assessing before the last manuscript iteration, and section 10 says the exit table is complete. This narrows a spec sentence, so it is reported to the user (CLAUDE.md product philosophy 3). The finding proposed `a.get("assessed_revision", 0)` so the fixture's directly written assessments stay readable; instead the fixture `write_round_assessment` writes `assessed_revision` (`self.store.revision + 1`, as `write_round` writes `admitted_revision`), because every record `assess_round` writes carries the key and no store holds a round assessment from before this release (CLAUDE.md section 2). The error text now reads "This round already has its assessment on this bundle". (2) Tests only: the `profile == "research"` filter of `has_round_exemplar` (Task 8 deviation 1) is pinned inside `test_an_unproductive_round_lacks_a_fresh_search_the_round_exemplar_or_a_new_cycle` by a verification-profile exemplar requirement recorded inside the round. The finding's optional assertion that `self.codes("research")` gains nothing from that requirement is not added: `literature._foundation_state` reads only the profile's own requirements (`full_requirements`), and the pin that follows in the test already requires the research profile to be ready (`readiness_required` otherwise), so the assertion would restate the pin.
- Task 8, fourth-review fixes: (1) `assess_round` refuses an assessment of any round but the latest admitted one (`invalid_round`, "Assess the current round"), the plan's check, placed before the `round_already_assessed` check. After `43c26fa` a closed round could be assessed again on a later round's bundle, and `assessment_for` (latest by `assessed_revision`) then replaced the assessment its closing decision was made on in every read path (`_reopening`, `_direction_open`, `record_round`), so a closed round's `successful` could be rewritten after the fact. Regression: `test_an_earlier_round_is_not_assessed_on_a_later_round_bundle` (round 2 assessed, round 3 admitted through `open_round(bundle, closes=2, ...)`, `run_round_work("r3")`, pin `paper-r3`, then the round-2 assessment on that bundle is `invalid_round` and `assessment_for` still returns the first assessment). (2) The record-id text-check sweep of the second-review fixes reaches the two places it missed: `_RoundEvidence.many` checks `review_id` as text before the `manuscript_review` lookup, and `_carried` checks `assessment_id`, `kind`, `question` and `cycle_id` as text before `_carried_key` hashes them (`kind` stays a text check, not a new vocabulary tuple: the membership test against `expected` already refuses an unknown kind, CLAUDE.md section 2b). Regressions: a list-valued `review_id` in candidate evidence (`test_review_evidence_names_a_manuscript_review_of_this_bundle`) and in a criterion's evidence (`test_every_criterion_and_stop_condition_is_judged_once_on_the_current_bundle`), and a carried item with each of the four keys as a list (`test_a_late_reassessment_of_an_earlier_cycle_is_carried_by_the_closing_round`) are `invalid_round`.
- Task 9: `rounds.round_state`, `rounds.round_summary`, the `round` gate, the two transition rules and the status merge follow the plan's reference code with these changes. (1) `decision` is set whenever an approved decision binds the current bundle, as the plan's interface sentence says; the plan's reference code (`settled = decision if ... not decision_obligations`) nulled it while `round_admission_missing` was pending, which contradicted the plan's own test (`report["decision"] == "continue"` beside `["round_admission_missing"]`). (2) The gate walks spec section 6 in order and stops at the first step that fails: the decision step (`round_decision_missing`, `round_review_missing`, `round_review_pending`, `round_admission_missing`) is reported only when the bundle is current and the closing round owes nothing, so an active round's report carries its productivity obligations and `round_assessment_missing` without a `round_decision_missing` that `record_round` would refuse anyway (the plan's code reported both). The case the plan left out is covered: a latest round assessed on a superseded bundle (Task 8, third review) reports `round_assessment_missing` with the round id, as `record_round` refuses it. (3) A changed opening claim without a marker (`changed_claim_ids`, Task 8 review) is folded into `round_claims_dropped` beside the dropped ids (`claim_ids` is the sorted union), since the obligation's text already names all three permitted outcomes; no new code was added to the vocabulary. `round_summary.dropped_claims` keeps the plan's definition (the dropped ids only). (4) `evaluate -> literature`: the plan's code (`if active_round is None: require_ready(round_state)`) would have let an approved stop enter literature, because a ready gate without an active round is a stop. Shipped: with no active round the transition is refused with `readiness_required` carrying the gate's `decision` and `obligations` (spec 6: only an admitted continue decision enters a round; `test_a_stop_does_not_enter_a_development_round`). With an active round the transition is permitted without evaluating the gate, as the plan wrote it: the spec's literal rule ("gate round ready with decision continue and an admission") cannot hold right after `round-admit`, because the gate then reports the new round's own obligations (spec 6 item 2). (5) `evaluate -> deposit` is keyed on `(source, target) == ("evaluate", "deposit")`, not the plan's `target == "deposit"`, so `deposit -> deposit` with status `done` does not re-run the round gate (spec 6 names the transition). (6) `report_views.order_key` was not added: `order_obligations` already sorts any iterable, so the status merge is `order_obligations({digest(o): o for o in merged}.values())` and `cli` imports `digest` (one line, CLAUDE.md section 6). The merge takes the readiness report's obligations, not the plan's `current_obligations` value (the preparation obligations when any), so the merged list holds only gate obligations and `next`/`obligations_page` keep reading `current_obligations`. (7) `admitted` is computed as the plan wrote it (an admission naming the reported decision), and it is always False on a report that carries a decision: `record_round` closes the current round number and an admission for that decision raises the current number by one, so the gate then looks for a decision closing the new number. The key is kept because spec 6 item 5 and the plan's test name it; it is reported to the user as dead surface (CLAUDE.md section 6) for a decision on the key or on the spec sentence. The review of Task 9 removed the key; see "Task 9, review fixes" (4). (8) `round_summary` reads the report's keys directly (no `.get` defaults): `round_state` always writes them. (9) Tests: the plan's `RoundGateTests.codes(report)` is `gate_codes(report)` (a static method), so it does not shadow the inherited `LiteratureCase.codes(profile)` with another signature; the plan's tests were appended with additions (`decision_id`, `next`, `ready`, `progress.cycles` and `measurement` on the gate report; `test_the_gate_reports_the_round_claims_and_the_stale_assessment` for `round_claim_missing`, `round_claims_dropped` on a dropped and on a changed claim, the assessed round's `round_decision_missing`, and `round_assessment_missing` after a re-pin; `test_a_stop_does_not_enter_a_development_round`; `test_the_round_summary_is_bounded_and_carries_the_active_round` on the summary's exact keys). The report-views case is `round_view()` (a summary at its widest: the four purposes, counts and measures at 10^9) copied into the worst-case bound test and asserted equal through `status_summary`; the plan's "10000-character strings in a progress list" is not what `round_summary` emits, as the plan's own parenthetical says. RED: eight of the ten new rounds tests failed (`invalid_gate`, `KeyError: 'round'`, `ResearchError not raised`, the status list) and the report-views case with `KeyError: 'round'`; the two passing ones pin record rules the transitions must keep.
- Task 9, review fixes: (1) Claims continuity is a round-gate obligation for the whole round of number 2 or more, assessed or not (spec section 9 states it as the round gate's rule, spec 6 item 2 conditions it on the round number). `round_state` derives the round's progress whenever a round was admitted and either it is active or a bundle is current, and reports `round_claims_dropped` (dropped plus changed ids) in both cases before the decision step, so an assessed round whose bundle drops an opening claim cannot reach `round_decision_missing`; `record_round` mirrors it (refused with `round_claims_dropped` and the `claim_ids`, right after the `round_assessment_missing` refusal), so the record and the gate agree. The rule has one home, `_unkept_claim_ids(progress)` and `_continuity_obligations(progress)`; `_progress_obligations(progress, admission, bundle)` takes the derived progress instead of deriving it. The report's `progress` is therefore also set for an assessed round on a current bundle (spec 14: the current round's counts). (2) An exhausted `development` budget is a round-gate obligation (spec 6 item 4, section 12): `_decision_obligations` prepends `resource_budget_exhausted` for the `development` purpose to every pending decision step (`round_decision_missing`, `round_review_missing`, `round_review_pending`, `round_admission_missing`) and to nothing when the approved decision is a stop, so the gate leads to the checkpoint-and-stop or raise-the-budget exit of spec 10 and an approved stop still passes. (3) `round_state` and `round_summary` carry `assessed` (spec 14): the latest round's latest assessment binds the current bundle. (4) `admitted` is removed from `round_state`, `round_summary` and the tests, the finding's primary fix, and the unreachable `if report["decision"] != "stop"` guard of the deposit transition is gone (a ready round gate is an approved stop: an active round always owes `round_assessment_missing`, an approved continue always owes `round_admission_missing`). The finding's alternative, reporting the admitted decision while the current bundle is the round's opening bundle, was probed in the worktree and is unreachable as well: right after `round-admit`, `publication._bundle` raises `readiness_required` (the active round's own literature obligations stale readiness, `test_the_gate_walks_from_bundle_to_decision_to_review_to_admission` shows `decision_obligations == ["readiness_required"]` there), and the first bundle the gate sees again is a re-pin whose digest differs from the opening one. The key was constant False in every reachable state under both readings, so its removal is dead-surface removal (CLAUDE.md section 6), not a product decision; spec 6 item 5 and spec 14 still name `admitted`, and this is the one open item for the user: to keep the spec sentence, re-add `"admitted": False` to `round_state` and `round_summary`, the key in `round_view()` of `tests/test_research_report_views.py` and in the `sorted(summary)` assertion, and amend the two spec sentences to say the value; otherwise the two spec sentences drop the word in Task 12. Task 10's packet reads neither key. Deviation (7) of Task 9 is closed by this. (5) The evaluate-stage status merge recomputes `counts.obligations` from the merged list, so `status --summary`'s count agrees with the obligations it shows. (6) Tests only: the pre-round deposit refusal asserts `details["action"] == "entering deposit"` with `["round_decision_missing"]`; the in-round deposit refusal says what it tests (`"evaluate completion"`, the stale readiness behind the publication gate); the active-round leg of the round rule is pinned in `test_the_gate_reports_the_round_claims_and_the_stale_assessment` on a bundle the publication gate accepts (`"entering deposit"`, `["round_assessment_missing"]`). (7) The `round_search_missing` and `round_exemplar_missing` obligations have one home, `rounds.literature_obligations(fresh_purposes, exemplar)`, called by `literature._foundation_state` (inside its `if active is not None` block, replacing its two inline copies) and by `_progress_obligations` (CLAUDE.md section 2b). The foundation report sorts its obligations by code, so the moved exemplar check changes no order. The interface list of the plan's Task 9 now reads: `round_state` keys `ready`, `obligations`, `decision_obligations`, `progress_obligations`, `round`, `active`, `assessed`, `decision`, `decision_id`, `next`, `progress`, `measurement`, `digest`, `counts`, `mechanical_only`; `round_summary` keys `number`, `active`, `assessed`, `decision`, `obligations`, `progress`, `measurement`. The second review of Task 9 added `limits` and `budget` to `round_state` and `limits`, `budget` and `usage` to `round_summary`; see the next entry.
- Task 9, second-review fixes: (1) The status carries the admitted round's `resource_limits`, the `development` budget line and the round's usage since its admission, which spec sections 12 and 14 name and the plan's narrower `round_summary` interface (plan line 1764) left out. `round_state` gains `limits` (the latest admission's `resource_limits`, None before any admission) and `budget` (`resources.account_report(records, "research").get("development")`: the per-unit `limit`, `charged`, `reserved` and `unknown` of the development account, None while neither a budget nor an account exists, so None before the first `round-admit`, whose charge opens the account). `round_summary` copies both and adds `usage`, the `literature` and `experiment` entries of `derive_progress`'s `usage` (the two purposes a round's limits name; None when no round was admitted). All three are numeric per unit, so the summary stays bounded; `round_view()` in `tests/test_research_report_views.py` carries them at 10^9 and the worst-case bound test still passes under 16 KiB. The fixture `write_round` now writes `resource_limits` on the admission, the key `admit_round` always writes. This follows the spec over the plan's interface list, so it is reported to the user (CLAUDE.md product philosophy 3); the alternative the review offered (keep the plan's interface and report limits and usage only in the Task 10 packet) was not taken because spec 12 names `status --summary` for the limits. (2) `round_state` derives the round's progress whenever a round was admitted, with or without a current bundle: an assessed round whose manuscript file changed before the re-pin (no active round, `publication_artifact_changed` from `publication._bundle`) reported `progress: None` while the same round on a current bundle reported its counts (spec 14: the current round's counts). The obligations are unchanged: `round_assessment_missing` and the continuity check still need a bundle, and `_continuity_obligations` on the empty claim lists that `derive_progress(..., None)` returns is `[]`. (3) The rule "the development budget is exhausted" has one home, `rounds._development_exhausted(records)`, read by `_next` (the `record_round` refusal) and `_decision_obligations` (the gate prefix); the two inline copies had drifted (`o.get("purpose")` against `o["purpose"]`), the class of defect the first review's Finding 7 fixed for the literature obligations (CLAUDE.md sections 2b and 13). No behavior change.
- Task 10: `review_packets.round_packet` and `review_delivery.deliver_round` follow the plan's reference code with these changes. (1) `round_packet(records, bundle, decision)` has no `report` parameter: the plan's `report.get("measurement")` was `round_state`'s `predictions.measurement_summary(records, bundle)`, so the packet computes the same value itself and `deliver_round` does not evaluate the round gate (CLAUDE.md sections 1 and 6). Task 11 wires `export --kind round` to `deliver_round` only, so no other caller is affected. (2) The latest review per assessor comes from `publication.latest_reviews` (Task 7's one home) instead of the plan's copied loop (CLAUDE.md section 2b). (3) `development` selects the closing round's cycle assessments by assessment revision (`assessed_revision` above the latest admission's `admitted_revision`, every assessment when no round was admitted), keyed by assessment id, the rule `rounds._carried` applies to the decision's carried developments (Task 4, second fixes; spec 1 and 5.1). The plan's cycle-id filter read only each cycle's latest assessment, so a re-assessment of an earlier round's cycle recorded in the closing round, which the decision under review must dispose of, would have been absent from the packet. This follows the spec over the plan, so it is reported to the user (CLAUDE.md product philosophy 3). Pinned by the second test: after round 2's work, `development` is exactly `assessment-cycle-1-r2` and `assessment-r2`, not `assessment-1`. (4) Each `rounds` entry carries `decision` (`id`, `payload`, `digest` of the decision that opened the round, read through the admission's `decision_id`): spec 11 lists "every earlier round's goal, assessment and decision", and the plan's interface list omitted the decision. Reported to the user as well; if the user confirms the plan's shape, the key and its assertion are removed. (5) A round's assessment is `rounds.assessment_for` (the latest by `assessed_revision`, Task 8 third fixes), not the plan's `next(...)` over the first match. (6) `deliver_round` reads the bundle through `publication._bundle`, as `record_round`'s `_prepare_context` and `predictions.record_prediction` do, so the refusal carries the precise code (`publication_bundle_missing`, `publication_readiness_stale` or `publication_artifact_changed`) instead of the plan's `publication_state(...)["bundle"] is None`, which folded every one of them into `publication_bundle_missing`. Task 11's CLI test (`publication_bundle_missing` before any pin) is unchanged by this. (7) The selected search's five keys are read as `search[k]`, not the plan's `search.get(k)`: `record_search` requires them (CLAUDE.md section 2). (8) The module imports `predictions`, `publication`, `resources` and `rounds` at the top, not inside the function as the plan wrote; there is no cycle (`rounds` does not import `review_packets`). The plan's two tests were appended with additions: `deliver_round` before any pin is `publication_bundle_missing` and after the pin without a decision is `round_decision_missing`; the packet's `manuscript.bundle_digest`, `decision.closes` and `decision.payload.id`, the reviews' kinds, the predictions' percentiles, `measurement` equal to `predictions.measurement_summary`, the empty `rounds` and `searches` before any admission, `synthesis` keys, `development` keys and the `literature` account; the second test goes on to assess round 2, record a stop on the new bundle and deliver the round packet, asserting the history entry's keys and values, the closing round's `development` keys, the two consequence searches' keys and the development account's charged round. RED: both tests errored with `ImportError: cannot import name 'deliver_round'`. Three one-caller helpers (`_round_history`, `_closing_round_development`, `_selected_searches`) were first extracted, then inlined back into `round_packet` (CLAUDE.md section 6) before the commit.
- Task 6, second-review fix (tests only, no code change): the profile half of the plan's interface sentence ("only while `rounds.active_round(records)` is not None and the profile is research") is pinned by one assertion in `test_an_active_round_requires_fresh_consequence_searches_and_an_exemplar`: with a round active, `foundation_report(store, "verification")` emits neither `round_search_missing` nor `round_exemplar_missing`. The active half was already pinned (six rounds tests fail with `readiness_required` without it).

## Second review of Task 9

Three findings, all major, all in `research_harness/rounds.py`: two spec-conformance gaps in the status report (Findings 1 and 2) and one duplicated rule (Finding 3). All fixed in the commit that carries this section; the details are in the deviations list ("Task 9, second-review fixes"). No finding was found wrong. RED before the code change: the two extended tests errored with `KeyError: 'budget'` and `KeyError: 'limits'`.

- Finding 1 (the round's `resource_limits`, the development budget line and the usage since admission reached no status view): fixed as the finding wrote it. `test_the_round_summary_is_bounded_and_carries_the_active_round` now reads the summary before any admission (`number` 1, `limits`, `budget`, `progress` and `usage` all None), then after `open_round` asserts `limits == admission["resource_limits"]`, the budget line `rounds == {"limit": None, "charged": 1, "reserved": 0, "unknown": 0}` (the admission's charge opened the account without a budget), `usage["literature"]["network_requests"] == 0`, the usage purposes within `{"literature", "experiment"}`, and the new `sorted(summary)` key list; after the pin a real `resources.charge` of 3 literature network requests shows as `usage["literature"]["network_requests"] == 3`. `test_an_exhausted_development_budget_is_a_round_gate_obligation` asserts the gate report's `budget["rounds"] == {"limit": 1, "charged": 1, "reserved": 0, "unknown": 0}`. The finding's "assert `summary["budget"] is None` before a budget is set" was placed before the admission: after `round-admit` the account exists and the line is reported with `limit: None`, which is the assertion above.
- Finding 2 (`round_state` derived progress only with an active round or a current bundle): fixed as the finding wrote it. Regression in the same test: after the assessment, `draft/paper.pdf` is rewritten and the summary reports `(active, assessed, progress.cycles) == (False, False, 1)`. Mutant (the old condition restored in isolation): the assertion errors with `TypeError: 'NoneType' object is not subscriptable`; the fixed file was restored byte-identical (`cmp`).
- Finding 3 (two drifted copies of the exhausted-development filter): fixed as the finding wrote it, `_development_exhausted(records)` with two callers. No behavior change; `test_an_exhausted_development_budget_is_a_round_gate_obligation` covers both callers.

## Review of Task 9

Seven findings, all major: four code defects in `research_harness/rounds.py` (Findings 1 to 4), one in `research_harness/cli.py` (Finding 5), one test gap in `tests/test_research_rounds.py` (Finding 6) and one duplicated vocabulary between `rounds.py` and `literature.py` (Finding 7). All fixed in the commit that carries this section; the details are in the deviations list ("Task 9, review fixes"). No finding was found wrong; Finding 4's alternative fix was found unreachable by a probe and its primary fix applied, which is reported to the user there. Every behavior change was checked against its mutant in the worktree (the fix reverted in isolation), with the file restored byte-identical afterwards (`cmp`):

- Finding 1, the gate: `progress_obligations = []` in the assessed branch makes `test_the_gate_reports_the_round_claims_and_the_stale_assessment` fail at `["round_claims_dropped"]` (the gate reports `round_decision_missing`). Finding 1, the record: the `round_claims_dropped` raise disabled in `record_round` fails the same test with `ResearchError not raised`.
- Finding 2: `round_decision_missing` without the exhausted prefix fails `test_an_exhausted_development_budget_is_a_round_gate_obligation` with `['round_decision_missing'] != ['resource_budget_exhausted', 'round_decision_missing']`. The test also pins the `record_round` refusal of a continue (a distinct goal statement, so `round_goal_repeated` does not fire first), the exhausted obligation beside `round_review_missing`, and a ready gate after an approved stop.
- Finding 3: `assessed = False` fails `test_the_round_summary_is_bounded_and_carries_the_active_round` with `(False, None, False) != (False, None, True)`. That test now walks the summary through the active round, a re-pin and the assessment (the round's work runs under the suffix `r2-work` because the test records `downstream-r2` first).
- Finding 5: the old merge line fails `test_status_at_evaluate_carries_publication_and_round_obligations_in_priority_order` with `0 != 1`.
- Findings 4, 6 and 7 change no behavior the existing tests could not see; the module runs cover them.

## Fourth review of Task 8

Three findings: one blocker (Finding 1, `research_harness/rounds.py`, record integrity) and two major (Findings 2 and 3, the same class of defect in `rounds.py`). All fixed in the commit that carries this section; no finding was found wrong. Each regression test was run on the shipped code before the fix (the shipped code is each fix's mutant, the added lines absent): Finding 1's test failed with `ResearchError not raised`, Finding 2's two tests and Finding 3's test errored with `TypeError: unhashable type: 'list'`; the fixed code passes all 48.

- Finding 1 (`assess_round` accepted an assessment of an earlier, closed round on a later round's bundle once `43c26fa` had narrowed `round_already_assessed` to the current bundle; the plan's "Assess the current round" check had been dropped in Task 8 deviation (2) on a ground that narrowing removed): fixed as the finding wrote it, `if latest_admission(records)["id"] != admission["id"]: raise ResearchError(_ERROR, "Assess the current round", {"round_id": admission["id"]})` before the `assessment_for` check. Regression `test_an_earlier_round_is_not_assessed_on_a_later_round_bundle` in `RoundAssessmentTests`, the finding's probe sequence. Deviation (2) of Task 8 is corrected.
- Finding 2 (`_RoundEvidence.many` looked up a list-valued `review_id` and raised `TypeError`; `cli.main` catches only `ResearchError`, `OSError` and `ValueError`): fixed by `_text(value["review_id"], "Review evidence ID")` after `_fields`. Regressions in the two tests the finding named (decision side through `record_round`, assessment side through `assess_round`).
- Finding 3 (`_carried` hashed unchecked `assessment_id`, `kind`, `question` and `cycle_id`): fixed as the finding wrote it, four text checks after `_fields`. Regression: four list-valued copies of the carried item in `test_a_late_reassessment_of_an_earlier_cycle_is_carried_by_the_closing_round`, each `invalid_round`.

## Third review of Task 8

Two findings, both major: one code defect in `research_harness/rounds.py` (Finding 1) and one test gap in `tests/test_research_rounds.py` (Finding 2). Both fixed in the commit that carries this section. No finding was found wrong; one detail of Finding 1's fix was replaced (the fixture writes `assessed_revision` instead of a `.get` default in production code; see the deviations list). Each new assertion was checked against its mutant in the worktree, with the fixed file restored byte-identical afterwards (`cmp`).

- Finding 1 (`assess_round` refused every second assessment while `record_round` requires one on the current bundle, so a round assessed on a superseded bundle had no exit): reproduced first by the new `test_a_round_assessed_on_a_superseded_bundle_is_assessed_again_on_the_current_one` in `RoundAssessmentTests` on the shipped code (a stop on the new bundle is `round_assessment_missing`, then the second assessment is `round_already_assessed`). Fixed as the finding wrote it: `assessment_for` returns the latest assessment by `assessed_revision`, and `assess_round` refuses only an assessment on a bundle the round's latest assessment already binds. The test asserts: a stop on the new bundle is `round_assessment_missing` while the assessment binds the superseded bundle; a second assessment on the new bundle is accepted with the round's id and the new digest, `rounds.assessment_for` returns it, the stop is then accepted, and a third assessment on the same bundle is `round_already_assessed`. Mutant (`assessment_for` back to `next(...)`, the first assessment): the second assessment is accepted but the test fails at `'round-2-assessment' != 'round-2-assessment-revised'`. `test_a_productive_round_is_assessed_with_derived_progress` keeps its same-bundle `round_already_assessed` assertion unchanged.
- Finding 2 (the `r["profile"] == "research"` clause of `has_round_exemplar` had no test; `reading.require_fulltext` accepts `purpose: exemplar` under `profile: verification`): fixed as the finding wrote it, inside `test_an_unproductive_round_lacks_a_fresh_search_the_round_exemplar_or_a_new_cycle`, which now records `exemplar-verification-r2` under the verification profile right after `open_round()`; the existing `stale_exemplar` block then observes the clause. Mutant (the clause removed): the test fails with `(True, False) != (False, True)`.

## Second review of Task 8

Six findings, all major: four code findings (Findings 1 and 5 name the same defect in `derive_progress`; Findings 2, 3 and 6 are in `rounds.py`, `publication.py` and `development.py`) and one test gap (Finding 4). All fixed in the commit that carries this section. One half of Finding 3 (closing the claim record) was found wrong and is not applied; see the deviations list. Every new assertion was checked against its mutant in the worktree, with the fixed file restored byte-identical afterwards (`cmp`).

- Findings 1 and 5 (`derive_progress` counted readings of every depth; spec 5.4 says full readings): fixed by `rounds.count_full_readings` on both sides of the difference and the opening key `fulltext_reading_count`. Regression in `test_a_productive_round_is_assessed_with_derived_progress`: an abstract reading of the exemplar's version (`abstract_note(self.links[1]["version_id"], "abstract-r2")`) is recorded right after `open_round()`, and `derived["readings"]` stays 1. Mutant (the all-depth expression on both sides): the test fails with `2 != 1`. The Task 5 assertion restates the definition (`sum(1 for r in before["reading"].values() if r["depth"] == "fulltext")`).
- Finding 2 (`assess_round` looked up a list-valued `round_id` and raised `TypeError`): fixed by `_text` before each lookup in `assess_round`, `record_round_review`, `_reopening` and `admit_round` (`round_id` and `review_id`). Regressions: `p.update(round_id=["round-2"])` in `test_every_criterion_and_stop_condition_is_judged_once_on_the_current_bundle`; a list-valued `round_id` in `test_the_review_is_independent_complete_and_bound_to_the_decision`; a list-valued reopening `round_id` in `test_a_repeated_round_goal_reopens_that_round_with_changed_evidence`; a list-valued `round_id` and, with the decision found, a list-valued `review_id` in `test_admission_needs_a_review_of_this_decision`. Each mutant (its `_text` line removed) errors with `TypeError: unhashable type: 'list'`.
- Finding 3 (the continuity markers were read by key presence and nothing validated their shape): fixed by `publication._claim_markers` at the pin. Regression `test_claim_continuity_markers_have_the_stated_shape` in `tests/test_research_publication.py`: `revised` as `null`, `true`, `{}`, with a blank `previous` or with an extra key, `superseded` as `{}` or with a null `reason`, and both markers on one claim are `publication_claims_missing`; the two stated shapes pin. Mutant (the `_claim_markers` call removed): eight subtests fail (`ResearchError not raised`, then `record_conflict` for the repeated bundle id). The claim record stays open; see the deviations list.
- Finding 4 (the `cycles` leg of `unproductive` had no isolating test): fixed as the finding sketched it, on a copied admission whose opening `cycle_ids` already lists `cycle-r2`. Mutant (`and cycles` removed): the test fails with `([], False) != ([], True)`.
- Finding 6 (`CRITERION_STATUSES` was a second copy of the cycle assessment's status vocabulary): fixed as the finding wrote it, `development.OBSERVATION_STATUSES`. No behavior change; `test_research_development.py` (65) and the `status="passed"` case in the rounds tests cover both callers.

## Review of Task 8

Five findings, all major: two code findings in `research_harness/rounds.py` (Findings 2 and 5) and three test gaps in `tests/test_research_rounds.py` (Findings 1, 3 and 4; Findings 1 and 4 name the same gap). All fixed in the commit that carries this section. No finding was found wrong; Finding 2 is a spec-versus-plan reading reported to the user (see the deviations list). Every new assertion was checked against its mutant in the worktree, with the fixed file restored byte-identical afterwards (`cmp`).

- Findings 1 and 4 (the `set(seen) != set(expected)` check of `_validate_judgments` and the evidence clause of spec 5.4 had no test; the fixture goal has one criterion): fixed by `test_every_criterion_of_a_two_criterion_goal_is_judged_with_evidence`, on a decision whose goal appends `sc-scope` (kind `scope`) and is admitted through `open_round(payload=...)`. An assessment that judges only `sc-wider` (`criteria.pop()`) and one whose `sc-wider` carries `evidence: []` are `invalid_round`; an assessment with `sc-wider` `observed` and `sc-scope` `unresolved` is accepted with `successful` True and both criteria in the stored payload. Mutant (the two check lines removed): the test fails with `ResearchError not raised`.
- Finding 2 (`derive_progress` compared claim ids only; spec 9 also requires the same text or a marker): fixed by `changed_claim_ids`, see the deviations list. `test_claims_continuity_marks_revised_superseded_and_dropped_claims` now pins a bundle with `bound` rewritten to "The maximum is 10." (`changed_claim_ids == ["bound"]`, neither revised nor dropped), asserts `[]` for the revised pin, and `[]` without a bundle. Mutant (`changed_claim_ids` fixed to `[]`): the test fails with `([], [], []) != (['bound'], [], [])`.
- Finding 3 (the `fresh` and `exemplar` legs of `unproductive` had no test): fixed by `test_an_unproductive_round_lacks_a_fresh_search_or_the_round_exemplar`, as the finding sketched it on the pure function. Mutant `True and exemplar and cycles`: fails with `(['downstream', 'exemplars', 'next_step'], False) != (..., True)`. Mutant without `exemplar and`: fails with `(False, False) != (False, True)`.
- Finding 5 (an unhashable criterion id raised `TypeError`): fixed by `_text(item["id"], name + " ID")` after `_fields`, and the edit `p["criteria"][0].update(id=["sc-wider"])` in `test_every_criterion_and_stop_condition_is_judged_once_on_the_current_bundle`. Mutant (the line removed): the test errors with `TypeError: unhashable type: 'list'`.

## Second review of Task 7

Two findings, both major, both in `research_harness/predictions.py`; no behavior change. Fixed in the commit that carries this section. No finding was found wrong.

- Finding 1 (`_SCORE_KEYS` was a second copy of the rubric's score vocabulary, whose home is `publication._review`; CLAUDE.md section 2b): fixed as the finding wrote it. `publication.SCORE_SCALES` is the module constant, `_review` iterates it, `predictions.measurement_summary` imports it and iterates `for key, _ in SCORE_SCALES`. The existing tests assert per key, so the key order change (`soundness` first instead of `overall`) is invisible; the new assertion `sorted(summary["reviews"]) == ["contribution", "count", "overall", "presentation", "soundness"]` pins that the summary carries exactly the rubric's keys.
- Finding 2 (`_cohort` re-validated what `_bundle` had proven; CLAUDE.md sections 2 and 6): the call chain in the finding was checked against the code (`_bundle` -> `_ready` -> `author_readiness_state` -> `readiness_state` -> `_Context` -> `synthesis_state` -> `_foundation_state` -> `cohort_report`, which emits `cohort_missing` at `cohort_evidence.py` for an empty list and for each id absent from `records["collection"]`; `_ready` raises `readiness_required` on any obligation) and holds: both reads use the same records snapshot, so once `_bundle` returned, the research scope's first collection id exists. Fixed by the finding's second option: `_cohort` (one caller) is inlined into `_prediction` as the single read. No test changed for this finding; the reachable `prediction_cohort_mismatch` path stays covered.

## Review of Task 7

Three findings from two independent reviewers, all major: one code finding (Finding 2, the selection duplicated between `predictions.measurement_summary` and `publication.publication_state`) and two test gaps that name the same gap (Findings 1 and 3: the latest-per-assessor selection of the summary had no test). All fixed in the commit that carries this section. No finding was found wrong; one detail of Finding 2's fix was not applied (below).

- Finding 2 (`research_harness/predictions.py` and `publication.py`): fixed by the public helper `publication.latest_reviews(records, bundle_digest)`, exactly the selection loop of `publication_state` without the `_review` call, used by both callers. The finding's "use `saved['core']` for the accept check" in `publication_state` was not applied: with it, `test_research_review_packets.py`'s `test_one_review_per_assessor_per_bundle_and_a_rejection_stands` fails at its first `assertTrue(... ["ready"])`, because its legacy record carries the copied rejection in `core` beside an acceptance artifact in `review`. The gate keeps the artifact-read core (`cores[saved["id"]]`); see the deviations list.
- Findings 1 and 3 (`tests/test_research_rounds.py`, the selection had no test): fixed by one sequence in `test_measurement_summary_reports_medians_and_spreads`, on Finding 3's casefold-equal later record and Finding 1's earlier record. The ids are `measure-one-1-later` and `measure-one-1-superseded`, and the superseded one is written last, so under both insertion and sorted iteration the earlier record is iterated last and an unconditional `latest[key] = saved` picks it. Checked against two mutations of `latest_reviews`: `latest[key] = saved` (unconditional) fails with spread `[1, 10]`, and `latest[saved["id"]] = saved` (count every review) fails at the first count assertion with 6; the shipped helper passes.

## Second review of Task 6

One finding, major, a test gap in `tests/test_research_rounds.py`; no code defect. Fixed in the commit that carries this section. The finding was not found wrong.

- Finding 1 (`literature._foundation_state`, the guard `latest = latest_admission(records) if profile == "research" else None` had no test: with it removed, the three task modules still passed while the verification profile of a study with an active round emitted `round_search_missing` for all four purposes and `round_exemplar_missing`): fixed as the finding wrote it. After `self.open_round()` the test asserts `self.assertFalse({"round_search_missing", "round_exemplar_missing"} & self.codes("verification"))`. Checked by removing the guard in the worktree: the test fails with `{'round_exemplar_missing', 'round_search_missing'} is not false`; the guard was restored (the file is byte-identical to `75e7360`) and the shipped code passes all 37.

## Review of Task 6

Four findings from two independent reviewers, all major: one code defect in `research_harness/literature.py` (Finding 3) and three test gaps in `tests/test_research_rounds.py`, two of which (Findings 2 and 4) name the same gap. All fixed in the commit that carries this section. No finding was found wrong.

- Finding 3 (`_foundation_state`, the check loop skipped a round's consequence searches once the round was assessed): fixed as the finding wrote it. `round_search_missing` is emitted before the loop, for every development purpose absent from `selected_searches` while `active is not None`; the loop iterates `selected_searches`, whose keys are the five purposes plus every development purpose selected in the current round (the `if purpose not in selected_searches ... continue` branch and its hidden invariant are gone). Regression: `test_development_searches_enter_the_judgments_that_synthesis_depends_on` now records an exemplar requirement after the round's assessment and asserts `downstream` among the `search_evidence_stale` purposes. RED on the shipped code (`'downstream' not found in ['recent', 'direct', 'theory', 'originals', 'adjacent']`), GREEN after the fix. `test_an_active_round_requires_fresh_consequence_searches_and_an_exemplar` is unchanged and still passes (the `round_search_missing` condition is the same).
- Finding 1 (a selected consequence search gets the same checks as the five purposes, no test): fixed by `test_a_fresh_consequence_search_is_judged_like_the_five_purposes`, written as the finding sketched it (`open_round`, `downstream` recorded in the round, an exemplar requirement, `search_evidence_stale` sorted equals the six purposes, then a re-recorded `downstream` is no longer stale). It passed on the shipped code, as the finding's probe said, because the round is active there; Finding 3's test covers the assessed case.
- Findings 2 and 4 (the `r["id"] not in active["opening"]["requirement_ids"]` clause of the exemplar rule had no test; the Task 5 record had deferred the non-empty `requirement_ids` case to Task 8, whose planned test asserts `derive_progress` instead): fixed by one test, `test_an_exemplar_the_round_opened_with_does_not_count`, on Finding 2's sequence through `open_round` (the real `admit_round` path with the carried alternative deferred) rather than Finding 4's `record_round` plus the fixture `write_round`. Checked by dropping the clause: the test then fails with `'round_exemplar_missing' not found in {'round_search_missing'}`; the clause was restored and the file is byte-identical to the fixed version. The Task 5 deviation sentence now names this test.

## Second review of Task 5

Three findings from two independent reviewers, all major, all test gaps in `tests/test_research_rounds.py`; no code defect. Fixed in the commit that carries this section. No finding was found wrong.

- Findings 1 and 2 (the same two branches of `admit_round`, `research_harness/rounds.py`: `unknown_round_decision` and `review["round_id"] != decision["id"]` had no test; commit `d937f32` claimed "test every admission refusal"): fixed by one new test, `test_admission_needs_a_review_of_this_decision`, written as Finding 1 sketched it (a second continue decision with another statement on the same bundle; `admit_round` with `round_id` `absent` is `unknown_round_decision`; the approval of the first decision named against the second is `round_review_required`; the approved decision itself is admitted). Finding 2's sketch inserted the same four lines into the existing opening-state test; the separate test keeps that test on the opening state. Checked against `admit_round` with the `review["round_id"] != decision["id"]` clause removed: the borrowed case fails with `ResearchError not raised`; the shipped code passes.
- Finding 3 (`opening.requirement_ids`, `reading_count` and `accounts` never asserted): fixed as the finding wrote it. `before = self.store.snapshot()["records"]` is taken before the admission; the test asserts `requirement_ids == sorted(before.get("fulltext_requirement", {}))` (empty in this fixture, see the deviations list), `reading_count == len(before["reading"])` (6) and `accounts == resources.account_report(before, "research")` with the literature account's 7 charged network requests. Each of the three was checked against its own mutation of `_opening` (`["ghost"]`, `0`, `{}`): every mutation fails the test; the shipped code passes all 32.

## Review of Task 5

Four findings from two independent reviewers, all major, in two pairs that name the same two defects. All fixed in the commit that carries this section. No finding was found wrong.

- Findings 1 and 3 (`research_harness/rounds.py` and this record: deviation (1) said the plan's `cycle` record kind does not exist): the claim was false, see the corrected deviation (1) above. Fixed by following the plan: `_opening` reads `sorted(records.get("cycle", {}))`, the lifecycle record the rest of the harness reads for cycles. No behavior change (the `cycle` and `cycle_plan` ids are identical); `test_admission_needs_an_approved_continue_decision_and_records_the_opening_state` still asserts `opening["cycle_ids"] == ["cycle-1"]`.
- Findings 2 and 4 (`tests/test_research_rounds.py`: six stated admission behaviors had no test): fixed by six new tests in `RoundAdmissionTests`, each written as its own test because a second approval on a bundle that already carries one is `round_decision_duplicate`. `test_a_stop_decision_opens_no_round` (`invalid_round`, spec 5.3 "a `continue` decision"); `test_admission_needs_the_decision_bundle_to_be_current` (`round_review_stale` from `admit_round` after a re-pin); `test_admission_needs_development_budget_room` (`resource_budget_exhausted` at admission after `set_budget` lowers `rounds` from 1 to 0 between the approval and the admission, spec 5.3 "the `development` budget room" and section 12); `test_admission_records_the_revision_and_limits_and_closes_only_the_current_round` (`admitted_revision` equals the store revision after the admission, `resource_limits` equals the proposal's, and `round_number_mismatch` for the approved decision once its round is assessed, the branch deviation (2) introduced); `test_a_field_change_adds_a_category_the_cohort_lacks` (`round_field_change_refused` for the cohort's own category, read from `records["collection"]`); `test_a_field_change_to_a_new_category_is_admitted` (the accepted path, `admission["goal"]["field_change"]` is `math.CO` in `arxiv`). Two fixture helpers were added to `tests/rounds_fixtures.py` because each now has more than two callers: `approve(payload)` records a decision and its approving review, and `open_round` uses it; `field_change_payload(bundle, corpus, category)` builds the horizontal decision, and the existing `test_a_field_change_stays_in_the_corpus_and_needs_literature_room` was rewritten onto it with the same assertions. Each new test was checked against a mutated `admit_round` (the refusal removed, `refuse=False` on the charge, `admitted_revision` off by one, the corpus check inverted): every mutation fails its test, and the shipped code passes all 31.

## Fourth review of Task 4

Two findings, both major, both fixed in the commit that carries this section. No finding was found wrong.

- Finding 1 (`research_harness/rounds.py`, `round_decision_duplicate` blocked a closing round for good once an approved decision existed on a superseded bundle): fixed by adding the condition `records["round_decision"][saved["round_id"]]["bundle_digest"] == bundle["digest"]` to the refusal. Regression test `test_an_approved_decision_on_a_superseded_bundle_does_not_block_the_round` in `RoundReviewTests`: pin, record and approve a stop, rewrite `draft/abstract.txt`, pin again, record a stop on the new bundle, approve it with `second-assessor`, assert `verdict == "approved"`. Checked by reverting the code fix in isolation: the test then fails with `round_decision_duplicate`. `test_one_approved_decision_per_closing_round` (same bundle) is unchanged and still passes. The spec rule is narrowed per bundle; see the deviations list.
- Finding 2 (`research_harness/rounds.py`, CLAUDE.md section 12: `admitted` bound a revision number, `reopened` bound a round id or None): renamed `admitted_revision` and `reopened_round_id`, including the `_distinct` and `_direction_open` parameters. No behavior change; the existing rounds tests cover the renamed paths.

## Third review of Task 4

Two findings, both major, both fixed in the commit that carries this section. No finding was found wrong.

- Finding 1 (`tests/test_research_rounds.py`, spec 5.1 "A deferred candidate of an earlier decision may become the goal of a later round" had no test): fixed by the new `test_a_deferred_candidate_may_become_a_later_goal`. It defers the transfer candidate in the round-1 decision, writes round 2 admitted and assessed successful (the fixture `write_round`), then records the round-2 decision pursuing that candidate as a horizontal goal and asserts the goal statement. This is one step more than the finding's sketch (which recorded the later decision without an admitted round in between) so that "a later round" is literal. The test was checked against a widened `_distinct` that scans every non-pursued candidate (`!= "pursue"` instead of `== "rejected"`): it fails with `round_goal_repeated`, and passes against the shipped code. No code change.
- Finding 2 (`research_harness/development.py`, `carried_developments(records, cycle_ids=None)` had no production caller passing `cycle_ids` after `5399725`): fixed by narrowing the signature to `carried_developments(records)` and the guard to `if cycle["assessment_id"] is None`. `tests/test_research_development.py`'s `test_a_next_round_branch_is_carried_by_cycle_id` became `test_a_next_round_branch_is_carried`: it calls the one-argument form, keeps the assertion on the returned branch item, and drops the `["other"] == []` assertion, which tested only the deleted filter. The plan's Task 2 interface line (`carried_developments(records, cycle_ids=None)`) is therefore a recorded deviation; no later plan task references the parameter.

## Pending findings

Recorded verbatim from the second review of Task 4 (two independent reviewers). Each is fixed in code with a regression test, never by weakening a test. All five are fixed in the commit that ticks them; the two behavior changes (Findings 1/3 and 4) were checked by reverting each fix in isolation and watching its test fail with "ResearchError not raised".

### Finding 1 (major): `research_harness/rounds.py`:211

- [x] fixed: `_reopening` reads the named round's assessment and refuses a missing or successful one; regression test `test_a_successful_round_is_not_reopened` (a new test rather than a case inside `test_a_repeated_round_goal_reopens_that_round_with_changed_evidence`, whose rounds are both unsuccessful). Same defect as Finding 3.

Summary: _reopening accepts a reopening of any assessed round, including one assessed successful. Spec 5.1: 'A goal that repeats an earlier withdrawn or unsuccessful goal is accepted only with next.reopening'; a successful round's goal has no reopening path and must be round_goal_repeated. Commit 4d352c7's message claims 'only a withdrawn or unsuccessful goal reopens' but the code never reads assessment['successful']. Confirmed empirically: with round-2 written successful=True on the current bundle, record_round accepted a closes=2 decision whose goal repeats round-2's goal with reopening round_id=round-2 (probe in the scratchpad, built on RoundsCase.write_round).

Fix: In _reopening replace lines 210-212 with: admission = records.get('round_admission', {}).get(value['round_id']); assessment = assessment_for(records, admission['id']) if admission else None; if assessment is None or assessment['successful']: raise ResearchError(_ERROR, 'Reopen an assessed unsuccessful earlier round'). _direction_open already restricts reopening to the two unsuccessful rounds, so nothing else changes. Add to test_a_repeated_round_goal_reopens_that_round_with_changed_evidence (tests/test_research_rounds.py) a case that writes the repeated round with successful=True and asserts invalid_round for a decision that reopens it.

### Finding 2 (major): `research_harness/rounds.py`:250

- [x] fixed: the unchanged-objective-with-lineage case is asserted in the new `test_the_next_round_is_well_formed` (Finding 5's test), not inside `test_a_decision_binds_the_exact_current_bundle_and_the_current_round`; one assertion covers both findings. No code change.

Summary: The guard 'An unchanged objective has no lineage' (lines 250-252; spec 5.1: next.objective identical to the configured objective has objective_lineage null) has no test in this task or in any later plan task (Task 5 tests only the widened path via open_round(objective=wider, lineage=lineage)). A stated behavior without a test.

Fix: In tests/test_research_rounds.py, RoundDecisionTests, add: same = self.decision_payload(bundle, lineage={'previous_id': self.objective['id'], 'containment': 'Unchanged.'}); self.assert_error('invalid_round', lambda: self.mutate(rounds.record_round, same)). One line inside test_a_decision_binds_the_exact_current_bundle_and_the_current_round is enough.

### Finding 3 (major): `research_harness/rounds.py`:211

- [x] fixed: same defect and same fix as Finding 1 (the error text is this finding's: "Reopen an earlier round that was assessed unsuccessful").

Summary: _reopening accepts any assessed round, including a successful one, as the reopened round. Spec 5.1 admits a repeated goal only when it repeats an earlier withdrawn or unsuccessful goal (both end as an assessment with successful: false); a goal that repeats a successful round's goal must stay round_goal_repeated. Today: write_round(..., successful=True) for round-2, then a decision closing 2 that repeats round-2's statement with reopening {round_id: 'round-2'} plus one review evidence item is accepted. The commit message applies the spec sentence only to rejected candidates.

Fix: In _reopening replace lines 210-212 with: admission = records.get('round_admission', {}).get(value['round_id']); assessment = assessment_for(records, admission['id']) if admission is not None else None; if assessment is None or assessment['successful']: raise ResearchError(_ERROR, 'Reopen an earlier round that was assessed unsuccessful'). Add to test_a_repeated_round_goal_reopens_that_round_with_changed_evidence (or a new test): self.write_round(first, 'round-2', successful=True, bundle_digest=bundle['digest']); repeated = self.decision_payload(bundle, closes=2, reopening={'round_id': 'round-2', 'reason': 'The picture changed.', 'evidence': changed}); self.assert_error('invalid_round', lambda: self.mutate(rounds.record_round, repeated)).

### Finding 4 (major): `research_harness/rounds.py`:138

- [x] fixed: `_carried` selects by `assessed_revision > latest["admitted_revision"]` (0 when no round was admitted); regression test `test_a_late_reassessment_of_an_earlier_cycle_is_carried_by_the_closing_round` follows the finding's scenario (decide, admit round 2 without assessment, re-assess cycle-1 with `next_round`, pin, assess round 2 on the new bundle, then `carried_development_missing` until the item is in `carried`). The finding calls this a plan-level choice the user may confirm: the spec text (sections 1 and 5.1) was followed; the plan's cycle-id filter is the deviation. `carried_developments` kept its `cycle_ids` parameter at this point; the third review removed it.

Summary: _carried selects the closing round's carried developments by cycle id (cycles absent from the admission's opening cycle_ids). A next_round item written during the closing round by a re-assessment of a cycle planned in an earlier round is therefore neither required (no carried_development_missing) nor accepted (key not in expected -> invalid_round), so the round gate cannot dispose of it. Spec 1 says 'The round gate must dispose of it' and 5.1 says 'every carried development recorded by any cycle assessment of the closing round'; section 7's `changes`/`contradicted` path returns claims to a cycle, which can be an earlier round's cycle, so the case is reachable. The plan's reference code prescribed the cycle-id filter, so this is a plan-level choice the user may confirm; the spec wording favors selecting by assessment time.

Fix: Select by assessment revision instead of cycle id: expected = {_carried_key(item): item for item in development.carried_developments(records) if latest is None or records['cycle_assessment'][item['assessment_id']]['assessed_revision'] > latest['admitted_revision']} (cycle_assessment records already carry assessed_revision, development.py:903; spec 5.3 and the plan's Task 5 record carry admitted_revision). In tests/rounds_fixtures.py write_round, write 'admitted_revision': self.store.revision + 1 on the admission (drop opening.cycle_ids if nothing else reads it) and let write_round write the assessment separately so a test can: decide, write_round('round-2') without assessment, self.recandidate('late') on cycle-1, bundle2 = self.pin(), write the round-2 assessment on bundle2, then assert_error('carried_development_missing', ...) for a closes=2 decision that omits assessment-late's alternative, and accept it once it is in carried.

### Finding 5 (major): `tests/test_research_rounds.py`:7

- [x] fixed: `test_the_next_round_is_well_formed` covers (a) number, (b) goal statement, (c) unchanged objective with lineage and `objective_locked` for a wrong `previous_id`, (d) `field_change` on a vertical goal; `test_a_review_binds_the_current_bundle` (a new test in `RoundReviewTests`) covers (e); `test_a_decision_needs_a_pinned_bundle` covers (f). All six passed against the existing code, so no code changed for this finding. The two other spec validations in `rounds.py` without a test here, `round_field_change_refused` and `resource_budget_exhausted` from `record_round`, are tested by the plan's Task 5.

Summary: Spec-stated validations of section 5.1/5.2 that rounds.py implements have no test in this task and none in a later plan task: (a) next.number must equal closes + 1 (rounds.py:248); (b) the goal must state the pursued candidate, same direction and normalized statement (rounds.py:163); (c) an unchanged objective with non-null objective_lineage is refused, and a widened objective goes through principles.widen_objective so a wrong previous_id raises objective_locked from record_round (rounds.py:250-254; Task 5 later tests only the happy path via open_round); (d) field_change on a vertical goal is refused (rounds.py:169); (e) round_review_stale when the decision's bundle is no longer the current bundle (second half of rounds.py:315; only the digest half is tested); (f) publication_bundle_missing for a decision recorded before any pin (spec: 'There is no decision without an exact manuscript').

Fix: Add test_the_next_round_is_well_formed to RoundDecisionTests: bundle = self.pin(); for each of the four payload edits assert invalid_round: p['next']['number'] = 3; p['next']['goal']['statement'] = 'Another statement.' (candidate unchanged); p['next']['objective_lineage'] = {'previous_id': self.objective['id'], 'containment': 'x'} with the unchanged objective; p['next']['goal']['field_change'] = {'corpus': 'arxiv', 'primaryCategory': 'math.CO'} on the default vertical goal; then wider = dict(self.objective, id='objective-wide', statement=self.objective['statement'] + ' and beyond') with lineage {'previous_id': 'absent', 'containment': 'x'} -> assert_error('objective_locked', ...). In RoundReviewTests add: decision on bundle one, rewrite draft/abstract.txt, self.pin() again, then assert_error('round_review_stale', lambda: self.mutate(rounds.record_round_review, self.review_payload(decision))). Add test_a_decision_needs_a_pinned_bundle: assert_error('publication_bundle_missing', lambda: self.mutate(rounds.record_round, self.decision_payload({'digest': '0' * 64}))).

